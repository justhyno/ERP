version: '2'
services:
  portalagent-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/portalAgent/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=portalAgent
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
